#include <iostream>
#include <string>
#include <stdio.h>
// #include <math.h>
// #include <string.h>
#include <stdlib.h>
#include <set>
#include <time.h>
#include <algorithm>



int swap(int * a, int * b);

int printArray(int * A, int from, int to);

int mySort(int k, int * A, int n);

int inspect(int * A, int size, int k);

int reOrder( int * A, int n);